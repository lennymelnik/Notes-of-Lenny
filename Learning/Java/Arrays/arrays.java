int[] arr; //declare a variable tha will hold an array of integers

arr = new int[10] //create a new array with the size of 10

System.out.println(ar.length); //check the length of the array

arr[0] = 4; //assign the value of 4 to the zero'th integer of the array
arr[1] = arr[0] + 5;//you can refer to other arrays when defining values or really doing anything

int[] arr = {1, 2, 3, 4, 5} //create an array with values in the same line
